#include "main.h"

/**
 * _strcat - function that concatenates two strings
 * @src: string function
 * @dest: to be appended
 * Return: dest;
 */

char *_strcat(char *dest, char *src)
{

	int i;
	int j;

	while (i = 0;  i  <='\0'; i++)

		while (j = 0; j  <= '\0' ;  j++)

		des[i] = src[j]+i
	des[i] = '\0';
	return (des);
}
